services:
    backend:
        build:
            context: ./backend
        ports:
            - "8000:8000"
        env_file:
            - ".env"
        volumes:
            - ./backend:/app/backend
            - ./database:/app/database:z
        working_dir: /app/backend
        environment:
            - PYTHONUNBUFFERED=1

    frontend:
        build:
            context: ./frontend
        ports:
            - "4200:4200"
        volumes:
            - ./frontend:/app
            - /app/node_modules
        depends_on:
            - backend
